Initialize `Vagrantfile`

```bash
vagrant init BOX_NAME
```

Create/start VM
```bash
vagrant up
```

Get VM status
```bash
vagrant status
```

Shutdown VM
```bash
vagrant halt
```

Destroy VM
```bash
vagrant destroy
```

Show SSH config
```bash
vagrant ssh-config
```

Access using SSH
```bash
vagrant ssh
```
